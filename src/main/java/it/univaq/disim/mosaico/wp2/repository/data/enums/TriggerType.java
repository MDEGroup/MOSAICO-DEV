package it.univaq.disim.mosaico.wp2.repository.data.enums;

/**
 * Enumeration representing how a benchmark run was triggered in MOSAICO taxonomy.
 */
public enum TriggerType {
    MANUAL,
    SCHEDULED,
    EVENT,
    WEBHOOK
}
