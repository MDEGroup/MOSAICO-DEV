package it.univaq.disim.mosaico.wp2.repository.data.enums;

/**
 * Enumeration for artifact types in MOSAICO taxonomy.
 */
public enum ArtifactType {
    TEXT,
    CODE,
    MODEL,
    TEST,
    ISSUE,
    DOCUMENTATION,
    BINARY
}