package it.univaq.disim.mosaico.wp2.repository.data.enums;

/**
 * Enumeration for collaboration patterns in MOSAICO taxonomy.
 */
public enum CollaborationPattern {
    CHAIN,
    BROADCAST,
    ROUND_ROBIN,
    MANAGER_WORKER
}