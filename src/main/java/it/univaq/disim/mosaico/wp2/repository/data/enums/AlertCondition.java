package it.univaq.disim.mosaico.wp2.repository.data.enums;

/**
 * Enumeration representing alert condition types in MOSAICO taxonomy.
 */
public enum AlertCondition {
    LESS_THAN,
    GREATER_THAN,
    EQUALS,
    NOT_EQUALS,
    PERCENTAGE_DROP,
    PERCENTAGE_RISE,
    ANOMALY_DETECTED
}
