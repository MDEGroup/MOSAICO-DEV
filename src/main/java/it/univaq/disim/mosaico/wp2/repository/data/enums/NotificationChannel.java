package it.univaq.disim.mosaico.wp2.repository.data.enums;

/**
 * Enumeration representing notification channel types in MOSAICO taxonomy.
 */
public enum NotificationChannel {
    EMAIL,
    SLACK,
    TEAMS,
    WEBHOOK,
    IN_APP
}
